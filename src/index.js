// TODO: create the User class/object
// it must support rank, progress and the incProgress(rank) method

class User {
  rank () {

  }
  progress () {

  }
  incProgress (rank) {
    return rank;
  }
}

export default User;
